Undefined means that the variable is assigned a memory space but not defined a type.
While, not defined means that the space is not allocated the space and will give error.

Check example.js for the files.